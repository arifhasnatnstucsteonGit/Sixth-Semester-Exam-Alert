<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sixsemesterexamalart"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.sixsemesterexamalart.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name="alarm" android:process=":remote"></receiver>
        <receiver android:name="alarm2" android:process=":remote"></receiver>
        <receiver android:name="alarm3" android:process=":remote"></receiver>
        <receiver android:name="alarm4" android:process=":remote"></receiver>
        <receiver android:name="alarm5" android:process=":remote"></receiver>
    </application>

</manifest>
